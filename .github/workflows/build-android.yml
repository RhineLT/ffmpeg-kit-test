name: Build Android

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'gradle'
        
    - name: Grant execute permission for gradlew
      run: chmod +x android/gradlew
      
    - name: Build simple-app (Debug)
      run: |
        cd android
        ./gradlew :simple-app:assembleDebug
        
    - name: Build simple-app (Release)
      run: |
        cd android
        ./gradlew :simple-app:assembleRelease
        
    - name: Upload APK artifacts (Debug)
      uses: actions/upload-artifact@v4
      with:
        name: simple-app-debug
        path: android/simple-app/build/outputs/apk/debug/*.apk
        retention-days: 7
        
    - name: Upload APK artifacts (Release)
      uses: actions/upload-artifact@v4
      with:
        name: simple-app-release
        path: android/simple-app/build/outputs/apk/release/*.apk
        retention-days: 7
        
    - name: Build summary
      run: |
        echo "### Android Build Summary :rocket:" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "#### Build Status: ✅ Success" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Built Applications:**" >> $GITHUB_STEP_SUMMARY
        echo "- simple-app (Debug)" >> $GITHUB_STEP_SUMMARY
        echo "- simple-app (Release)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Artifacts uploaded and available for download.**" >> $GITHUB_STEP_SUMMARY
